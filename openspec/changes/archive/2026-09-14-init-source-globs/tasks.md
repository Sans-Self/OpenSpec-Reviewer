# Tasks: init-source-globs

## 1. Renderer

- [x] 1.1 `render` leaves extensions in `NO_CITATIONS` (`json`) out of
      `source_globs`; `path_extensions` keeps them.
- [x] 1.2 A test with `json` found asserts it is in `path_extensions`
      and not in `source_globs`.
