# Tasks: pairing-vocabulary

## 1. The glossary

- [x] 1.1 `pairing` lists `pair` as its only deprecated synonym, and its
      meaning names `match` as the verb.

## 2. The prose

- [x] 2.1 `semantic-diff`, `input-sources` and `change-model` say `match`
      where they said `pair` as a verb.
- [x] 2.2 "the name pair" and "a pair of names" name the two things.

## 3. The names

- [x] 3.1 `change-model § A rename is a pair of names` and
      `semantic-diff § Every delta entry pairs with canon by name` are
      renamed, and the four test titles that quote them follow.

## 4. The lint

- [x] 4.1 `openspec-reviewer lint` reports no warnings here.
