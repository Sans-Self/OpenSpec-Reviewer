# Tasks: init-elixir

## 1. Candidates

- [x] 1.1 `ex`, `exs`, `heex` in the candidate extensions; `_build`,
      `deps` in the default skip directories; a test with an `.exs`
      file under `test/` asserts the glob is written.
