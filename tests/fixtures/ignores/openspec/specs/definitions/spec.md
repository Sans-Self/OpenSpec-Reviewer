# definitions

## Requirements

### Requirement: route

A spec MUST use `route` to mean:

A path the router mounts a page on.

#### Scenario: Used in a spec

- **WHEN** a spec says route
- **THEN** it means this path
