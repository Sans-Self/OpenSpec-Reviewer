//! spec:alpha § A route records its mountType
//! spec:beta § A mount names its feature

/// spec:alpha § Rows are ordered by path
pub fn rows() {}

/// spec:gamma § Entries are appended to the ledger
pub fn append() {}
