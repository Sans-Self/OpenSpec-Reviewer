import { cite } from '../../fixtures/cite'

test(`spec:key-rotation § Members re-encrypt on their next write`, () => {})
test(`spec:key-rotation § Rotation keeps a grace period`, () => {})
test("spec:key-rotation § A rotation records the operator's device", () => {})
test(
  cite(
    'key-rotation',
    'Rotation produces a new epoch key',
  ),
  () => {},
)
